var React = require('react');

export default class Base extends React.Component{
    render () {
        return (
            <div>
                <h1> Header </h1>
            </div>
        );
    }
}